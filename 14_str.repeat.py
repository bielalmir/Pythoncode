# Program for string repetition

# Program for string repetition

text = input("Enter a string: ")
times = int(input("Enter how many times to repeat: "))

repeated = (text + " ")* times

print("Repeated String:", repeated)
